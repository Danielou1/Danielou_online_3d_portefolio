{
  "version": 2,
  "functions": {
    "server.ts": {
      "runtime": "@vercel/node@2.0.0"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/server.ts" },
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}